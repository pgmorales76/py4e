# Given that `fruit` is a string, what does `fruit[:]` mean?
# Because you're omitting, both, the first, and second, indexes,`fruit[:]`, means the whole string.

fruit = 'banana'

print(fruit[:]) # You'd never do this, in practice! It's, syntactically, there for completeness.