# Write a function called, `chop`, which takes a list, and modifies it,
# removing the first and last elements, and returns `None`.
# Then, write a function called, `middle`, that takes a list,
# and returns a new list, that contains all, but the first, and last elements.

# def chop(t):
#     del t[0]
#     del t[-1]
#     return None

# letters = ['a', 'b', 'c']
# chop(letters)
# print(letters)

# def middle(t):
#     return t[1:-1] # Returning a list w/out first, or last element. The first index value is returned, but not the second one!

# first_list = ['My', 'favorite', 'author', 'is', 'Rand']
# a_new_list = middle(first_list)
# print(first_list)
# print(a_new_list)